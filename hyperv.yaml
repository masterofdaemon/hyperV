services:
  logger:
    binary: "/bin/bash"
    args: ["./tests/logger.sh"]
    workdir: "/Users/masutanoakira/projs/hyperV"
    auto_restart: true
  worker:
    binary: "/bin/bash"
    args: ["./tests/worker.sh"]
    workdir: "/Users/masutanoakira/projs/hyperV"
    auto_restart: false
  crasher:
    binary: "/bin/bash"
    args: ["./tests/crasher.sh"]
    workdir: "/Users/masutanoakira/projs/hyperV"
    auto_restart: true
  surrealdb:
    binary: "/bin/bash"
    args:
      - "-lc"
      - "surreal start --bind $SURREAL_HOST:$SURREAL_PORT rocksdb:$SURREAL_STORAGE_PATH --log $SURREAL_LOG_LEVEL --user $SURREAL_USER --password $SURREAL_PASSWORD"
    env:
      SURREAL_HOST: "0.0.0.0"
      SURREAL_PORT: "8000"
      SURREAL_STORAGE_PATH: "/tmp/surreal_data"
      SURREAL_LOG_LEVEL: "info"
      SURREAL_USER: "root"
      # Set the real password via the SURREAL_PASSWORD environment variable
      # (or a .env file in workdir); it is expanded at load time so the
      # secret is never committed to the repo.
      SURREAL_PASSWORD: "${SURREAL_PASSWORD:-changeme-set-SURREAL_PASSWORD-in-environment}"
    auto_restart: true
